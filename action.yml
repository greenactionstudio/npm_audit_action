name: 'npm audit action'
description: 'Runs npm audit'
inputs:
  severity:
    description: 'severity to use'
    required: false
    default: 'low'
  title:
    description: 'string to look for in the title'
    required: false
    default: Null

runs:
  using: 'node16'
  main: 'index.js'